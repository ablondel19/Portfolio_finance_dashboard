import { useTheme } from "@mui/material";

export const ChartMargin = {
  top: 0,
  right: 0,
  left: 0,
  bottom: 0,
};

export const CustomDot = {
  stroke: "#f0f0f0",
  strokeWidth: 3,
  r: 1.25,
};
